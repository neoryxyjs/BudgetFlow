[phases.setup]
nixPkgs = { nodejs = "18", jdk = "17" }

[phases.install]
cmds = ["cd backend && mvn clean install -DskipTests"]

[phases.build]
cmds = ["cd backend && mvn clean package -DskipTests"]

[start]
cmd = "cd backend && java -jar target/budgetflow-backend-1.0.0.jar"

